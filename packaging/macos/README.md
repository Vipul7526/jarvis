# macOS DMG

This folder reserves the macOS distribution boundary. A real DMG requires a macOS build runner, a notarization-capable Apple Developer account, signed application bundles, and a verified local service integration. The current Linux environment cannot produce or sign a genuine macOS DMG, so no binary is claimed here.

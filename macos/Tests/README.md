# macOS Native Tests

This folder contains XCTest coverage for the Swift bridge contracts. Run `swift test` from `macos/` on a macOS host with Swift 5.9 or newer. Linux CI can review the files but cannot substitute for Apple SDK, signing, or notarization validation.
